<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ExamenPGG.UI.MainPage"
             xmlns:viewmodel="clr-namespace:ExamenPGG.UI.ViewModel"
             x:DataType="viewmodel:MainViewModel"
             WidthRequest="1024" HeightRequest="768">

    <Grid RowDefinitions="576, auto"
          ColumnDefinitions="auto,720">

        <ContentView Content="{Binding GameBoardView}"
                     Grid.Column ="1"/>

        <ContentView Content="{Binding InformationView}">
            
        </ContentView>

        <ContentView Content="{Binding GameControlView}"
                     Grid.Row="1"
                     Grid.ColumnSpan="2">
        
        </ContentView>
                     
                     

    </Grid>
    
</ContentPage>
